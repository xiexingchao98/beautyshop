// invoked in worker
// think.beforeStartServer(async () => {
//   if (true) {
//     let models = think.app.models
//     for (let modelName in models) {
//       think.model(modelName).init()
//     }
//   }
// })